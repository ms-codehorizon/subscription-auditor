[project]
name = "subscription-auditor"
version = "0.1.0"
description = "An AI operator whose brain is auditable markdown"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.109.1",
    "flask>=3.1.3",
]
